# The backends Shall ships with, as ROWS IN THE SAME TABLE a user adds a row to — parsed by the
# same loader, wired by the same `build_capabilities`, reading with the same named readers.
# `setting_stores.toml` states the reason in its own header: an adapter mechanism the built-ins
# bypass is one nobody has tested, and this was the table with sixty rows doing the bypassing.
#
# A row is `[[backend]]`. Every field is the one `adapters/backends.toml` documents; nothing here
# is a built-in-only escape hatch, which is the point — U2 says a custom backend is a first-class
# peer of a built-in, and a peer is what the same file makes it.
#
# `reads` / `searches` name a reader in `src/parsers/named.rs`, each of which is a function with a
# fixture behind it. `[backend.parser]` describes a shape instead, and is what a user writes for a
# manager Shall has never seen. A row may use either; a row with `list_args` and neither is caught
# by `every_row_can_read_what_it_asks_for` in the suite, because a listing nobody can parse reads
# as an empty machine and `sync` answers that by installing everything.
#
# `os` restricts a row to one platform. It is read through `AdapterRow::applies_to`, like every
# other table's, so a Windows row is visible to a Linux build and can be asserted there.
#
# `[backend.fixture]` carries the bytes the manager printed, what the reader must make of them,
# and a `source` saying where they came from. It goes LAST in a row: it is a table header, so
# every key after it belongs to the fixture — `deny_unknown_fields` is what turns a `searches`
# that slid below the block into a load error instead of a silently lost capability.
#
# WHAT IS NOT HERE, and why. Five shapes a row cannot hold, each carrying a closure that reaches
# into a manager's own answer:
#
#   * an install conditional on a read (`snap`'s `@classic`, `flatpak`'s `@channel`);
#   * argv that is a program rather than a template (`nix`, `go`, `storage`);
#   * a removal that is a filesystem operation informed by a query (`appimage`, `web`, `github`);
#   * a `PropertyProbe` reaching INTO JSON rather than taking a command's whole stdout;
#   * per-package identity that includes the version (`asdf`'s tool@version, kept in Rust for
#     its `after_required` pin only — see below, it IS a row).
#
# Those stay in `registry.rs` and in their own modules, and the list of which is asserted by
# `backend_is_data_not_code_tests.rs`, whose exemption table carries the reason for each.


# ---------------------------------------------------------------------------------------------
# Language and ecosystem managers. Cross-platform: gated by their binary being on PATH, not by
# the OS, which is why none of these carries `os =`.
# ---------------------------------------------------------------------------------------------

# PHP / Packagist.
[[backend]]
name = "composer"
install_args = ["global", "require"]
remove_args = ["global", "remove"]
list_args = ["global", "show", "--format=json"]
search_args = ["global", "search", "--format=json"]
upgrade_args = ["global", "update"]
reads = "language"
searches = "language"
# composer prints `Changed current directory to …` ahead of the JSON, which the reader steps
# past — a strict parse would report nothing outdated on every machine.
outdated_args = ["global", "outdated", "--format=json"]
outdated_reads = "composer_outdated"
# composer keeps every downloaded package archive under `~/.composer/cache`.
clean_cache_args = ["clear-cache"]
version_pin = { kind = "inline", template = "{name}:{version}" }
# `composer global show` lists the whole solved tree. `--direct` would report just the requested
# packages, but no test image covers composer, so decline to adopt rather than ship a guess.
#
# `installed` is the array; the vendor prefix is part of the name and must survive.
[backend.fixture]
source = "docker composer:2, `composer global require psr/log monolog/monolog` then `composer global show --format=json`, 2026-08-09"
list = '''
{
    "installed": [
        {
            "name": "monolog/monolog",
            "direct-dependency": true,
            "homepage": "https://github.com/Seldaek/monolog",
            "source": "https://github.com/Seldaek/monolog/tree/3.10.0",
            "version": "3.10.0",
            "description": "Sends your logs to files, sockets, inboxes, databases and various web services",
            "abandoned": false
        },
        {
            "name": "psr/log",
            "direct-dependency": true,
            "homepage": "https://github.com/php-fig/log",
            "source": "https://github.com/php-fig/log/tree/3.0.2",
            "version": "3.0.2",
            "description": "Common interface for logging libraries",
            "abandoned": false
        }
    ]
}
'''
expect = ["monolog/monolog@3.10.0", "psr/log@3.0.2"]

# OCaml.
[[backend]]
name = "opam"
install_args = ["install", "-y"]
remove_args = ["remove", "-y"]
list_args = ["list", "--installed", "--short"]
search_args = ["search", "--short"]
upgrade_args = ["upgrade", "-y"]
reads = "names_only"
searches = "names_only"
# `opam clean` removes downloaded archives and build logs, never a switch's installed packages —
# which is the line X.3 draws between a cache and a removal.
clean_cache_args = ["clean"]
version_pin = { kind = "inline", template = "{name}.{version}" }

# `--short` is bare names and no versions, which is why the row reads with `names_only`
# rather than something that would take the second column of a line that has none.
[backend.fixture]
source = "docker ocaml/opam:debian-12-ocaml-5.1, `opam list --installed --short`, 2026-08-09"
list = '''
base-bigarray
base-domains
base-nnp
base-threads
base-unix
ocaml
ocaml-base-compiler
ocaml-config
ocaml-options-vanilla
opam-depext
'''
expect = [
    "base-bigarray",
    "base-domains",
    "base-nnp",
    "base-threads",
    "base-unix",
    "ocaml",
    "ocaml-base-compiler",
    "ocaml-config",
    "ocaml-options-vanilla",
    "opam-depext",
]
# opam installs dependencies as packages; `--roots` would be the right wiring, unverified here.

# Lua.
[[backend]]
name = "luarocks"
install_args = ["install"]
remove_args = ["remove"]
list_args = ["list", "--porcelain"]
search_args = ["search", "--porcelain"]
reads = "ws_name_version"
searches = "ws_name_version"
# The version is a bare operand, not an option: `luarocks install --` answers `Error: missing
# argument 'rock'`, and `luarocks install -- <rock> <version>` is identical to the same line
# without the terminator (measured, `tools` image 2026-08-04). So the terminator stays on both
# the pinned and the unpinned path, which is why this says `After` rather than naming a flag.
version_pin = { kind = "after", args = ["{version}"] }

# Tab-separated `name version status tree`. The version carries luarocks' own revision
# suffix (`-3`) and it is part of the version, not decoration: `luarocks install say 1.4.1`
# and `say 1.4.1-3` are different requests.
[backend.fixture]
source = "docker alpine:3.19 with lua5.4 + luarocks, `luarocks-5.4 install say` then `luarocks-5.4 list --porcelain`, 2026-08-09"
list = '''
say	1.4.1-3	installed	/usr/local/lib/luarocks/rocks-5.4
'''
expect = [
    "say@1.4.1-3",
]

# Nim. No CLI search, no upgrade-all.
[[backend]]
name = "nimble"
install_args = ["install", "-y"]
remove_args = ["uninstall", "-y"]
list_args = ["list", "--installed"]
reads = "nimble"
version_pin = { kind = "inline", template = "{name}@{version}" }

# The version is inside a bracketed record, not a second column: `nimpy  [(version: 0.2.1,
# checksum: ...)]`. Two spaces separate the name from it.
[backend.fixture]
source = "docker nimlang/nim:2.0.2, `nimble install nimpy` then `nimble list --installed`, 2026-08-09"
list = '''
nimpy  [(version: 0.2.1, checksum: 22173fb24ce9ca9d1c1db63fe15bdfb14e69c76a)]
'''
expect = [
    "nimpy@0.2.1",
]

# pixi global environments.
[[backend]]
name = "pixi"
install_args = ["global", "install"]
# pixi removes a global TOOL with `global uninstall`; `global remove` deletes a package from an
# environment and requires `--environment`, so it errors on a bare tool name.
remove_args = ["global", "uninstall"]
list_args = ["global", "list"]
search_args = ["search"]
# `global upgrade-all` was removed upstream; pixi 0.73 answers it with "This command has been
# removed, please use `pixi global update` instead". A plan-smoke passed it the whole time,
# because constructing an argv proves nothing about whether the argv exists.
upgrade_args = ["global", "update"]
reads = "pixi"
searches = "pixi"
# pixi prints its listing as a box-drawing tree; `--json` is the same answer already parsed.
# Recent pixi only, hence the negotiation rather than a straight swap.
machine_list_args = ["global", "list", "--json"]
machine_list_reads = "pixi_json"
manual = "all_installed"
version_pin = { kind = "inline", template = "{name}={version}" }

# A box-drawing tree, not a table: the environment is `name: version` under a heading, and
# the `exposes:` line beneath it lists executables.
[backend.fixture]
source = "docker ghcr.io/prefix-dev/pixi:latest, `pixi global install ripgrep` then `pixi global list`, 2026-08-09"
list = '''
Global environments as specified in '/root/.pixi/manifests/pixi-global.toml'
└── ripgrep: 15.2.0 
    └─ exposes: rg
'''
expect = [
    "ripgrep@15.2.0",
]

# Spack, for HPC.
[[backend]]
name = "spack"
install_args = ["install"]
remove_args = ["uninstall", "-y"]
list_args = ["find", "--format", "{name} {version}"]
search_args = ["list"]
# Two different readers, which is why `reads` and `searches` are separate fields: `spack find`
# prints `name version` and `spack list` prints bare names.
reads = "ws_name_version"
searches = "names_only"
version_pin = { kind = "inline", template = "{name}@{version}" }

# The shape is not a guess: `list_args` passes spack the format string that produces it,
# so `{name} {version}` is what comes back by construction. What is unverified is only
# whether a real `spack find` prints anything else around it.
[backend.fixture]
source = "UNVERIFIED: written from the `--format` string this row itself passes; spack/ubuntu-jammy has no spack on PATH and no packages installed"
list = '''
zlib 1.2.13
hdf5 1.14.3
'''
expect = [
    "zlib@1.2.13",
    "hdf5@1.14.3",
]

# Elixir/Hex archives. The global-archive surface; project-scoped hex deps are out of scope.
[[backend]]
name = "mix"
install_args = ["archive.install", "hex", "--force"]
# `--force` on the removal too, and not for symmetry: measured, a bare `mix archive.uninstall
# phx_new` with no terminal prints `Are you sure…? [Yn]`, takes the empty answer, EXITS 0 and
# leaves the archive installed. Shall reported a removal that did not happen — the scoop-exit-0
# shape (E7), one manager over.
remove_args = ["archive.uninstall", "--force"]
list_args = ["archive"]
reads = "mix_archive"
manual = "all_installed"
# The version is a bare positional after the name. Not optional in practice — an archive declares
# which Elixir it supports, so on Elixir 1.14 the newest `phx_new` fetches, builds and then
# refuses to run, and pinning is the only way to install one at all (measured, 2026-07-29).
version_pin = { kind = "after", args = ["{version}"] }

# An empty listing that is a sentence, which is the case a reader gets wrong in the expensive
# direction: `No archives currently installed.` read as one package named `No` is drift, and read
# as unreadable is a backend that refuses to plan.
[backend.fixture]
source = "docker elixir:1.16, `mix archive` on a container with no archives, 2026-08-09"
list = '''
No archives currently installed.
'''
expect = [
]

# Helm plugins. Chart releases are a different concept and out of scope.
#
# A declaration is `helm:NAME@url=SOURCE`: helm installs a plugin from a URL but lists and
# uninstalls it by the name in its `plugin.yaml`, so naming the URL would install a plugin Shall
# could never remove or recognise again (U39).
[[backend]]
name = "helm"
install_args = ["plugin", "install"]
remove_args = ["plugin", "uninstall"]
list_args = ["plugin", "list"]
reads = "ws_name_version"
manual = "all_installed"
install_source_option = "url"

# A cluster with no plugins prints the header and nothing else. That is an EMPTY listing,
# not an unreadable one, and the difference is whether every verb that needs the installed
# set refuses.
[backend.fixture]
source = "docker alpine/helm:3.14.0, `helm plugin list` with no plugins installed, 2026-08-09"
list = '''
NAME	VERSION	DESCRIPTION
'''
expect = [
]

# Haskell / cabal. No uninstall verb, so `remove_args` is empty and removal reports Unsupported.
[[backend]]
name = "cabal"
install_args = ["install"]
list_args = ["list", "--installed", "--simple-output"]
search_args = ["list", "--simple-output"]
reads = "cabal"
searches = "ws_name_version"
version_pin = { kind = "inline", template = "{name}-{version}" }

# Kept verbatim to the eighth package: the three chatter lines are the reason `cabal_list`
# exists rather than `ws_name_version`, and a fixture without them would not have found it.
[backend.fixture]
source = "docker haskell:9.6, `cabal list --installed --simple-output` on a container with no cabal config yet, 2026-08-09 (first eight packages of thirty-eight kept)"
list = '''
Config file path source is default config file.
Config file not found: /root/.config/cabal/config
Writing default configuration to /root/.config/cabal/config
Cabal 3.10.3.0
Cabal-syntax 3.10.3.0
array 0.5.8.0
base 4.18.3.0
binary 0.8.9.1
bytestring 0.11.5.4
containers 0.6.7
deepseq 1.4.8.1
'''
expect = [
    "Cabal@3.10.3.0",
    "Cabal-syntax@3.10.3.0",
    "array@0.5.8.0",
    "base@4.18.3.0",
    "binary@0.8.9.1",
    "bytestring@0.11.5.4",
    "containers@0.6.7",
    "deepseq@1.4.8.1",
]

# Haskell / stack. Like cabal it has no uninstall verb, and no reliable global install list —
# `cannot_list` is an ERROR rather than an empty vector, because "this machine has nothing" is a
# fact the planner acts on and "this question cannot be asked" is not.
[[backend]]
name = "stack"
install_args = ["install"]
reads = "cannot_list"
version_pin = { kind = "inline", template = "{name}-{version}" }

# The three Node managers, which spell one shape three ways: the global flag (`-g` vs a `global`
# verb), the install verb, the remove verb, and where each keeps what it installed. None has a
# usable CLI search — npm's is slow and output-unstable, pnpm has none, yarn removed its own in
# Berry — and all three resolve from the npm registry, which is `search_source`. Upgrading is
# re-installing each global package, for all three.
[[backend]]
name = "npm"
install_args = ["install", "-g"]
remove_args = ["uninstall", "-g"]
list_args = ["list", "-g", "--depth=0", "--json"]
upgrade_reinstall_args = ["install", "-g"]
search_source = "npm_registry"
reads = "language"
# `npm outdated` exits non-zero when it FINDS something, so this is read through `run_output`
# rather than a status-checked reader.
outdated_args = ["outdated", "-g", "--json"]
outdated_reads = "npm_outdated"
# `npm cache clean` refuses without `--force`; the flag is npm asking whether you meant it, not a
# way to make it try harder.
clean_cache_args = ["cache", "clean", "--force"]
manual = "all_installed"
version_pin = { kind = "inline", template = "{name}@{version}" }
# `npm prefix -g` reports the PREFIX, not the module directory, and the layout below it differs
# by OS. Getting this wrong yields a path that does not exist.
[[backend.property_probes]]
property = "install_path"
args = ["prefix", "-g"]
template = "{base}/lib/node_modules/{name}"
windows_template = "{base}/node_modules/{name}"

# npm names the enclosing project and puts the globals under `dependencies`, one object each.
[backend.fixture]
source = "docker node:20, `npm list -g --depth=0 --json`, 2026-08-09"
list = '''
{
  "name": "lib",
  "dependencies": {
    "corepack": {
      "version": "0.34.6",
      "overridden": false
    },
    "npm": {
      "version": "10.8.2",
      "overridden": false
    }
  }
}
'''
expect = ["corepack@0.34.6", "npm@10.8.2"]

[[backend]]
name = "pnpm"
install_args = ["add", "-g"]
remove_args = ["remove", "-g"]
list_args = ["list", "-g", "--depth=0", "--json"]
upgrade_reinstall_args = ["add", "-g"]
search_source = "npm_registry"
# `pnpm list -g --json` returns an ARRAY of project objects where npm returns one bare object.
# The shared reader handles both; parsing pnpm as npm yields nothing.
reads = "language"
outdated_args = ["outdated", "-g", "--json"]
outdated_reads = "npm_outdated"
# pnpm's content-addressable store, minus what is still referenced.
clean_cache_args = ["store", "prune"]
manual = "all_installed"
version_pin = { kind = "inline", template = "{name}@{version}" }
# `pnpm root -g` already IS the global node_modules directory, so the package folder is
# `<root>/<name>`; appending another `node_modules` yields a path that does not exist.
[[backend.property_probes]]
property = "install_path"
args = ["root", "-g"]
template = "{base}/{name}"
[[backend.property_probes]]
property = "bin_path"
args = ["bin", "-g"]
template = "{base}"

# An ARRAY of project objects where npm prints one bare object, and the globals hang off
# `dependencies` inside the first of them.
[backend.fixture]
source = "docker node:20 with PNPM_HOME set, `pnpm add -g cowsay` then `pnpm list -g --depth=0 --json`, 2026-08-09"
list = '''
[
  {
    "path": "/root/.pnpm/global/5",
    "private": false,
    "dependencies": {
      "cowsay": {
        "from": "cowsay",
        "version": "1.6.0",
        "resolved": "https://registry.npmjs.org/cowsay/-/cowsay-1.6.0.tgz",
        "path": "/root/.pnpm/global/5/.pnpm/cowsay@1.6.0/node_modules/cowsay"
      }
    }
  }
]
'''
expect = [
    "cowsay@1.6.0",
]

[[backend]]
name = "yarn"
# yarn 1 spells global as a leading verb rather than a flag.
install_args = ["global", "add"]
remove_args = ["global", "remove"]
# Not `--json`: yarn 1's JSON stream reports the BINARIES a global package installed
# (`{"type":"bins-catj","items":["catj"]}`) and never the package, so the one line that names it
# is the plain output's. Measured on a host with `catj` installed.
list_args = ["global", "list"]
upgrade_reinstall_args = ["global", "add"]
search_source = "npm_registry"
reads = "language"
# `yarn cache clean` with no package name empties the whole cache, in Classic and in Berry.
clean_cache_args = ["cache", "clean"]
manual = "all_installed"
version_pin = { kind = "inline", template = "{name}@{version}" }
# `yarn global dir` returns the folder CONTAINING node_modules, unlike `pnpm root -g`.
[[backend.property_probes]]
property = "install_path"
args = ["global", "dir"]
template = "{base}/node_modules/{name}"
[[backend.property_probes]]
property = "bin_path"
args = ["global", "bin"]
template = "{base}"

# The one line that names the package is the `info "name@version" has binaries:` line. Everything
# around it is chatter, and the `- name` lines under it are executables.
[backend.fixture]
source = "docker node:20, `yarn global add cowsay` then `yarn global list`, 2026-08-09"
list = '''
yarn global v1.22.22
info "cowsay@1.6.0" has binaries:
   - cowsay
   - cowthink
Done in 0.16s.
'''
expect = ["cowsay@1.6.0"]

# Rust / crates.io.
#
# The one thing worth carrying: `cargo install foo` on an already-installed foo declines and exits
# 0. Upgrading has to say `--force`, which is why `upgrade_reinstall_args` carries args of its own
# instead of being a boolean — a boolean would have upgraded cargo by asking it to do nothing, and
# reported success.
[[backend]]
name = "cargo"
install_args = ["install"]
remove_args = ["uninstall"]
list_args = ["install", "--list"]
search_args = ["search"]
upgrade_reinstall_args = ["install", "--force"]
# `cargo install --list` indents each crate's binaries beneath it; a column reader would take
# those indented lines as package names.
reads = "language"
searches = "language"
manual = "all_installed"
# `--version` is an option of `install`, not of the crate name, so it goes ahead of the terminator
# and the name stays protected behind it.
version_pin = { kind = "before", args = ["--version", "{version}"] }

# `name vVERSION (source):` then one indented line per binary. The binaries are not
# packages, and the `v` is cargo's decoration rather than part of the version.
[backend.fixture]
source = "docker rust:1-slim, `cargo new /t` then `cargo install --path /t` then `cargo install --list`, 2026-08-09"
list = '''
t v0.1.0 (/t):
    t
'''
expect = [
    "t@0.1.0",
]

# Python applications in their own venvs.
[[backend]]
name = "pipx"
install_args = ["install"]
remove_args = ["uninstall"]
list_args = ["list", "--json"]
upgrade_args = ["upgrade-all"]
reads = "language"
manual = "all_installed"
version_pin = { kind = "inline", template = "{name}=={version}" }
[[backend.property_probes]]
property = "install_path"
args = ["environment", "--value", "PIPX_HOME"]
template = "{base}/venvs/{name}"

# The package is four levels down, under `venvs.<env>.metadata.main_package`, and the version is
# `package_version` rather than `version`. `venvs: {}` on an empty box is a true answer, not an
# unreadable one — which is the distinction `or_unrecognised_json`'s `Some(0)` arm exists for.
[backend.fixture]
source = "docker python:3.12-slim, `pipx install cowsay` then `pipx list --json`, 2026-08-09"
list = '''
{
    "pipx_spec_version": "0.1",
    "venvs": {
        "cowsay": {
            "metadata": {
                "backend": "pip",
                "environment": "cowsay",
                "exposure_enabled": true,
                "injected_packages": {},
                "main_package": {
                    "apps": [
                        "cowsay"
                    ],
                    "include_apps": true,
                    "include_dependencies": false,
                    "package": "cowsay",
                    "package_or_url": "cowsay",
                    "package_version": "6.1",
                    "pinned": false,
                    "pip_args": [],
                    "suffix": ""
                },
                "pipx_metadata_version": "0.12",
                "python_version": "Python 3.12.13",
                "venv_args": []
            }
        }
    }
}
'''
expect = ["cowsay@6.1"]

# uv's tool installs.
[[backend]]
name = "uv"
install_args = ["tool", "install"]
remove_args = ["tool", "uninstall"]
list_args = ["tool", "list"]
upgrade_args = ["tool", "upgrade", "--all"]
reads = "uv_tool_list"
# uv keeps wheels and source distributions under its own cache directory.
clean_cache_args = ["cache", "clean"]
manual = "all_installed"
version_pin = { kind = "inline", template = "{name}=={version}" }
[[backend.property_probes]]
property = "install_path"
args = ["tool", "dir"]
template = "{base}/{name}"

# One line per tool, then one `- name` line per executable it exposed. The executables are not
# packages: read as `name version`, `- ruff` is a package called `-` at version `ruff`.
[backend.fixture]
source = "docker python:3.12-slim, `uv tool install ruff` then `uv tool list`, 2026-08-09"
list = '''
ruff v0.16.2
- ruff
'''
expect = ["ruff@0.16.2"]

# krew, the kubectl plugin manager. Its verbs are subcommands of `kubectl`.
#
# `extra_probes` is the thing 193 lines of hand-written Rust knew and the table did not: krew is a
# PLUGIN, so `kubectl` alone is not enough. A host with kubectl and no krew reported READY and
# then failed every command with `unknown command "krew"` — including `shall update`, which
# refreshes every backend at once.
[[backend]]
name = "krew"
binary = "kubectl"
extra_probes = ["kubectl-krew"]
install_args = ["krew", "install"]
remove_args = ["krew", "uninstall"]
list_args = ["krew", "list"]
search_args = ["krew", "search"]
upgrade_args = ["krew", "upgrade"]
update_args = ["krew", "update"]
# `kubectl krew list` prints `PLUGIN  VERSION` (older versions: bare names); `search` prints
# `NAME  DESCRIPTION  INSTALLED`, so only the first column is a name.
reads = "ws_name_version"
searches = "names_only"
manual = "all_installed"

# The header is dropped by `is_noise_line`, which is why an empty plugin list reads as
# empty rather than unreadable.
[backend.fixture]
source = "UNVERIFIED: written from krew's upstream README; installing krew needs a cluster-shaped kubectl"
list = '''
PLUGIN
ctx
ns
'''
expect = [
    "ctx",
    "ns",
]
# krew installs the index's current version and has no per-install version pin.

# Dart / pub, reached through `dart pub global`.
[[backend]]
name = "pub"
binary = "dart"
install_args = ["pub", "global", "activate"]
remove_args = ["pub", "global", "deactivate"]
list_args = ["pub", "global", "list"]
# pub.dev has no upgrade-all verb: re-activating each package unpinned is the upgrade. Upgradable
# without being searchable — pub.dev has no CLI search.
upgrade_reinstall_args = ["pub", "global", "activate"]
reads = "ws_name_version"
manual = "all_installed"
# pub pins with a trailing positional version — an operand, so `--` stays in front of both it and
# the name.
version_pin = { kind = "after", args = ["{version}"] }

# `name version`, one per line, with no header and no decoration.
[backend.fixture]
source = "docker dart:stable, `dart pub global activate http webdev` then `dart pub global list`, 2026-08-09"
list = '''
http 1.6.0
webdev 3.8.1
'''
expect = [
    "http@1.6.0",
    "webdev@3.8.1",
]

# asdf version manager. A tool/plugin is the "package"; installing pins a version.
[[backend]]
name = "asdf"
install_args = ["install"]
remove_args = ["uninstall"]
list_args = ["list"]
reads = "asdf"
manual = "all_installed"
# asdf refuses to install without a version, so an unpinned line asks for `latest`. Removal needs
# none: measured, `asdf uninstall nodejs` returns 0 and the version leaves `asdf list`.
version_pin = { kind = "after", args = ["{version}"], unpinned = "latest" }

# The plugin name is the package; the indented lines under it are the versions of that
# plugin, and a plugin with versions installed is one package, not one per version.
[backend.fixture]
source = "UNVERIFIED: written from asdf's documented `asdf list` layout; the git-clone install failed in an alpine container"
list = '''
nodejs
  20.11.0
python
  3.12.1
'''
expect = [
    "nodejs",
    "python",
]


# ---------------------------------------------------------------------------------------------
# Linux distribution managers that are pure argv. The rest of the Linux family — apt, dnf,
# pacman, apk, zypper, xbps — carry an orphan dry-run, a dependency probe or an essential-set
# reader whose parsers ARE rows too; they are here for the same reason the others are.
# ---------------------------------------------------------------------------------------------

# GNU Guix. Per-user, no root needed.
[[backend]]
name = "guix"
os = "linux"
install_args = ["install"]
remove_args = ["remove"]
# `guix package -I` lists the profile's manifest — what was explicitly installed — not the store
# closure behind it.
list_args = ["package", "-I"]
search_args = ["search"]
upgrade_args = ["upgrade"]
reads = "ws_name_version"
searches = "guix"
manual = "all_installed"
version_pin = { kind = "inline", template = "{name}@{version}" }

# Four columns, tab-separated -- `name version output store-path` -- and each field is PADDED
# WITH SPACES to a column width before its tab. The literal shape is `hello    <TAB>2.12.3 <TAB>
# out<TAB>...`, so a reader that split on the tab alone would carry `hello    ` as the name.
# `ws_name_version` splits on whitespace and is right; the fixture below is the real bytes
# rather than the tidy ones, because the tidy ones would have agreed with a wrong reader too.
#
# The third and fourth columns are the output name and the store path, and neither is a
# version.
[backend.fixture]
source = "captured from `guix package -I` in metacall/guix:latest (guix 43833af) on 2026-08-14 -- the run that also closed this backend's proving.rs exemption"
list = '''
nss-certs	3.101.4	out	/gnu/store/v9i76lmhx0rlq46wyypv62wh2s3igqcf-nss-certs-3.101.4
hello    	2.12.3 	out	/gnu/store/ab584kfyc7pymc1cmdrkwzz3lwv86yf6-hello-2.12.3
sed      	4.9    	out	/gnu/store/w7cxnqm5g12jnn1nm86dapmc6l4k40aa-sed-4.9
'''
expect = [
    "nss-certs@3.101.4",
    "hello@2.12.3",
    "sed@4.9",
]

# Gentoo Portage. Installed packages come from `qlist -I` (portage-utils).
[[backend]]
name = "emerge"
os = "linux"
install_args = ["--ask=n", "--quiet"]
remove_args = ["--unmerge", "--ask=n"]
list_binary = "qlist"
list_args = ["-I"]
search_args = ["--search"]
upgrade_args = ["--update", "--deep", "--newuse", "--ask=n", "@world"]
reads = "names_only"
searches = "emerge"
# Portage's names are `category/name`, and `qlist -I` reports them that way — so a line reading
# `jq` and a machine holding `app-misc/jq` are one package under two spellings. Without this,
# `Searchable::lookup` compares the string the user typed against an atom and never matches, so
# no bare name on this backend resolves at all (`J8`). The category is not dropped: it is what
# reaches emerge's argv, because Portage refuses a bare `emerge jq` as ambiguous.
qualified_names = true
needs_root = true
is_exclusive = true

# `list_binary` is qlist, not emerge: portage has no listing verb and `qlist -I` is the one
# portage-utils ships for it. Category-qualified atoms, one per line, no versions.
[backend.fixture]
source = "UNVERIFIED: written from `qlist -I`'s documented output; gentoo/stage3 was not pulled"
list = '''
app-editors/vim
dev-lang/python
sys-apps/portage
'''
expect = [
    "app-editors/vim",
    "dev-lang/python",
    "sys-apps/portage",
]
# Portage's @world file is the explicit set; `emerge -I` lists the whole tree (306 packages vs an
# empty world on the gentoo test image). Wiring the world file is the right fix; until it is
# verified, adopt nothing.

# Solus eopkg.
[[backend]]
name = "eopkg"
os = "linux"
install_args = ["install", "-y"]
remove_args = ["remove", "-y"]
list_args = ["list-installed"]
search_args = ["search"]
upgrade_args = ["upgrade", "-y"]
reads = "eopkg"
searches = "eopkg"
needs_root = true
is_exclusive = true
# Solus keeps fetched `.eopkg` archives in `/var/cache/eopkg/packages`.
clean_cache_args = ["delete-cache"]

# `name - Short description`, and the description is prose that must not become part of the
# name.
[backend.fixture]
source = "UNVERIFIED: written from eopkg's documented `list-installed` layout; the Solus image name in the harvest script did not resolve"
list = '''
bash - The GNU Bourne Again SHell
coreutils - The GNU core utilities
'''
expect = [
    "bash",
    "coreutils",
]
# eopkg installs dependencies and `list-installed` reports them all, so adopt nothing.

# Slackware slackpkg. Installed packages are read from `/var/log/packages`.
[[backend]]
name = "slackpkg"
os = "linux"
install_args = ["-batch=on", "-default_answer=y", "install"]
remove_args = ["-batch=on", "-default_answer=y", "remove"]
list_binary = "ls"
list_args = ["-1", "/var/log/packages"]
search_args = ["search"]
upgrade_args = ["-batch=on", "-default_answer=y", "upgrade-all"]
reads = "slackpkg"
searches = "slackpkg"
needs_root = true
is_exclusive = true
# Slackware does no dependency resolution: every installed package was chosen.
manual = "all_installed"

# Not a manager's output at all: `ls -1 /var/log/packages` lists Slackware's package
# database, one `name-version-arch-build` file per package. The last three dash-separated
# fields are the version, arch and build, so the name is everything before them — which is
# why a name containing dashes survives.
[backend.fixture]
source = "UNVERIFIED: written from Slackware's /var/log/packages naming convention; no Slackware image was reachable"
list = '''
vim-9.0.1-x86_64-1
aaa_base-15.0-x86_64-3
xorg-server-21.1.8-x86_64-1
'''
expect = [
    "vim",
    "aaa_base",
    "xorg-server",
]

# AerynOS moss. Content-addressable system package manager; atomic transactions over /usr.
# Needs root and is exclusive (moss holds its own store lock). Linux only: it requires
# renameat2(2) and namespace isolation that are not available elsewhere.
#
# **The fixture is UNVERIFIED** — no AerynOS container was reachable when this row was
# written. `names_only` is chosen because moss's installed listing is expected to carry one
# package name per line, possibly with a version column that names_only safely drops. The
# fixture MUST be replaced with captured real output before this backend can be proven.
[[backend]]
name = "moss"
os = "linux"
install_args = ["install"]
remove_args = ["remove"]
list_args = ["list", "installed"]
search_args = ["search"]
upgrade_args = ["sync", "--update"]
reads = "names_only"
searches = "names_only"
needs_root = true

[backend.fixture]
source = "UNVERIFIED: written from the documented CLI surface at aerynos.dev/users/system-management/moss-state-management/; no AerynOS image was reachable"
list = '''
bash
coreutils
systemd
'''
expect = [
    "bash",
    "coreutils",
    "systemd",
]
